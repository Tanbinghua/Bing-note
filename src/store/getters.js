const getters = {
  language: state => state.language,
  theme: state => state.theme,
  id: state => state.id,
  name: state => state.name,
  email: state => state.email,
  create_time: state => state.create_time,
}
export default getters
